export class FakultasEntity {
  constructor({ fakultas_id, kode_fakultas, nama_fakultas }) {
    this.id = fakultas_id;
    this.kode_fakultas = kode_fakultas;
    this.nama_fakultas = nama_fakultas;
  }
}
